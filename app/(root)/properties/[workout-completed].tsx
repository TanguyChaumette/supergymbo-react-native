import React from "react";
import { Text, View } from "react-native";

const WorkoutCompleted = () => {
  return (
    <View>
      <Text>WorkoutCompleted</Text>
    </View>
  );
};

export default WorkoutCompleted;
